// Stub for the `@carnaby/api` type-only path alias (see tsconfig.base.json).
// A later task wires up tRPC here and exports `type AppRouter` for apps/web
// to consume. Kept empty (not a module augmentation) until then.
export {};
