// shadcn/ui's generated components import `cn` from "@/lib/utils" — keep that
// contract as a re-export so future `shadcn add` output works untouched, while
// the app's own code keeps importing from lib/cn.ts directly.
export { cn } from './cn';
